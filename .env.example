# 이 키는 절대 깃에 올리지 마세요. (.gitignore 에 .env 포함됨)
# 배포: Vercel 대시보드 > Settings > Environment Variables 에 아래를 등록.
# 로컬에서 `vercel dev` 로 테스트할 때만 이 파일을 .env 로 복사해 채웁니다.
ANTHROPIC_API_KEY=sk-ant-여기에-본인-키
# (선택) 모델 변경 - 기본값: claude-haiku-4-5-20251001
# ANTHROPIC_MODEL=claude-sonnet-4-6
